{
  "name": "voluum-cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "voluum": "dist/index.js"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "bun test",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@toon-format/toon": "^2.1.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.13.10",
    "typescript": "^5.9.3"
  }
}
